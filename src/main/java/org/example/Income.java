package org.example;

public class Income /*extends Transaction */{

   /*
    private Enum category;

    public Income(double amount, String date, Enum category) {
        super(amount, date);
        this.category = category;
    }

    public Enum getCategory() {
        return category;
    }
    */







    /*
    Denna klass skall ärva Transaction och skall minst ha följande fält:
    * category- ska vara ett Enum med kategorier för inkomster. = EIncomeCategory
    fält= dessa definieras i klassen och representerar tillstånd => state


    kod att ha i metoden för att ta bort och får fram index på elementen i arraylist
    int index=0;
    for(String s : list)
    string s = Income income : list = min arraylist för income/Expense
    System.out.println((index++)+": "+s = income);

     */
}
